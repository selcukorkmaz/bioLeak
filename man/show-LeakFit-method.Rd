% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_resample.R
\name{show,LeakFit-method}
\alias{show,LeakFit-method}
\title{Display summary for LeakFit objects}
\usage{
\S4method{show}{LeakFit}(object)
}
\arguments{
\item{object}{A \code{LeakFit} object.}
}
\value{
No return value, called for side effects (prints a brief summary
  to the console). Returns \code{object} invisibly.
}
\description{
Prints a brief one-screen summary of a LeakFit, including
  task and outcome, fold count and status (successful, skipped, failed),
  and the headline cross-validated metric. Use \code{summary()} for the
  full per-fold diagnostic report.
}
\examples{
set.seed(1)
df <- data.frame(
  subject = rep(1:6, each = 2),
  outcome = rbinom(12, 1, 0.5),
  x1 = rnorm(12),
  x2 = rnorm(12)
)
splits <- make_split_plan(df, outcome = "outcome",
                      mode = "subject_grouped", group = "subject", v = 3,
                      progress = FALSE)
custom <- list(
  glm = list(
    fit = function(x, y, task, weights, ...) {
      stats::glm(y ~ ., data = as.data.frame(x),
                 family = stats::binomial(), weights = weights)
    },
    predict = function(object, newdata, task, ...) {
      as.numeric(stats::predict(object,
                                newdata = as.data.frame(newdata),
                                type = "response"))
    }
  )
)
fit <- fit_resample(df, outcome = "outcome", splits = splits,
                    learner = "glm", custom_learners = custom,
                    metrics = "auc", refit = FALSE, seed = 1)
show(fit)
}
